<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.finalapplication.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment
        android:id="@+id/prowerb_Button"
        android:name="com.example.finalapplication.proverb_Button"
        android:label="fragment_prowerb__button"
        tools:layout="@layout/fragment_prowerb__button" >
        <action
            android:id="@+id/action_prowerb_Button_to_mainActivity"
            app:destination="@id/mainActivity" />
    </fragment>
</navigation>